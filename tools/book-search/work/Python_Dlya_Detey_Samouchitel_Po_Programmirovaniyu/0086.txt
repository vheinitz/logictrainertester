>>> testfunc('Мэри')
                                 Привет, Мэри


                           Также можно создать функцию, которая принимает не один, а два,
                       три или иное количество аргументов (либо, напротив, не принимает ар-
                       гументы). Например, вот функция testfunc с двумя аргументами:


                                 >>> def testfunc(fname, lname):
                                         print('Привет, %s %s' % (fname, lname))


                          При вызове функции значения аргументов следует писать через
                       запятую:


                                 >>> testfunc('Мэри', 'Смит')
                                 Привет, Мэри Смит


                          В качестве аргументов можно указывать переменные (которые перед
                       этим нужно создать):

First name — имя                 >>> firstname = 'Джо'
Last name —                      >>> lastname = 'Робертсон'
фамилия                          >>> testfunc(firstname, lastname)
                                 Привет, Джо Робертсон


                           Зачастую из функции полезно вернуть какое-то значение. Это делает-
Return — вернуть       ся с помощью команды return. Например, представьте такую функцию
                       для расчета сбережений:

Savings —                        >>> def savings(pocket_money, paper_route, spending):
сбережения                               return pocket_money + paper_route – spending


                           Эта функция принимает три аргумента. Вычисляя результат, она
Pocket money —         складывает первые два аргумента — pocket_money и paper_route, —
карманные              а затем вычитает из полученного значения третий аргумент — spending.
деньги
Paper route —
                       Команда return возвращает результат вычислений в ту часть кода, отку-
выручка                да функция была вызвана, и его можно сохранить в переменной (обыч-
за доставку газет      ным способом, с помощью знака "=") либо вывести на экран:
Spending — рас-
ходы
                                 >>> print(savings(10, 10, 5))
                                 15




86    Часть I. Учимся программировать