def draw(self):
                                  self.canvas.move(self.id, self.x, self.y)
                                  pos = self.canvas.coords(self.id)
                                  if pos[1] <= 0:
                                       self.y = 1
                                  if pos[3] >= self.canvas_height:
                                       self.y = -1



                            В строке  мы поменяли вызов функции move, теперь передаем в нее
Pos — позиция,         свойства x и y. В строке  создали переменную pos, поместив в нее зна-
положение              чение, полученное от функции холста coords. Эта функция возвращает
Coords — коорди-
                       x- и y-координаты любой фигуры на холсте по ее идентификатору. В дан-
наты
                       ном случае мы передаем в функцию coords свойство id, где хранится
                       идентификатор круга (мяча).
                            Функция coords возвращает координаты в виде списка из четырех
                       чисел. Если вывести его на экран, мы увидим нечто подобное:


                                 print(self.canvas.coords(self.id))
                                 [255.0, 29.0, 270.0, 44.0]


                           Первые два числа (255.0 и 29.0) — это координаты левого верхнего
                       угла прямоугольника, в который вписан наш круг (x1 и y1), а вторые два
                       (270.0 и 44.0) — координаты правого нижнего угла прямоугольника (x2
                       и y2). Этими значениями мы и воспользуемся в следующих строках кода.
                                                  В строке  сравниваем координату y1 (это
                                              верх мяча) с нулем и, если она меньше или равна
                                              0, задаем свойству y значение 1. Таким образом
                                              мы говорим, что если мяч достиг верхней грани-
                                              цы холста, нужно прекратить двигать его вверх,
                                              перестав вычитать 1 из его вертикальной коорди-
                                              наты. Вместо этого мы будем прибавлять к этой
                                              координате 1, чтобы мяч сменил направление.
                                                  В строке  сравниваем координату y2 (это
                                              низ мяча) со свойством canvas_height и, если
                                              y2 больше или равно canvas_height, снова за-
                                              даем свойству y значение –1.
                                                  Запустите этот код, и вы увидите, как мяч ле-
                                              тает вверх-вниз, отскакивая от границ, до тех пор
                                              пока вы не закроете окно.




200    Часть II. Пишем игру «Прыг-скок!»