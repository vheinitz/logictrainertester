Выражения, состоящие из нескольких строк кода (например, кон-
                        струкции if), выполнить, как правило, не удастся:


                                  >>> eval('''if True:
                                          print("это не сработает")''')
                                  Traceback (most recent call last):
                                    File "<pyshell#35>", line 2, in <module>
                                      print("this won't work at all")''')
                                    File "<string>", line 1
                                      if True:
                                       ^
                                  SyntaxError: invalid syntax


                             Функция eval часто используется, чтобы обработать ввод пользова-
                        теля как Python-выражение. Например, можно написать простую про-
                        грамму-калькулятор, которая запрашивает математическое выражение
                        и с помощью eval вычисляет результат.
                             Введенное пользователем выражение считывается как строка, и для
                        его вычисления нужно преобразовать строку в числа и операторы. Функ-
                        ция eval сделает это автоматически:


                                  >>> your_calculation = input('Введите выражение: ')
                                  Введите выражение: 12*52
                                  >>> eval(your_calculation)
                                  624



                            Мы использовали input для считывания введенной пользователем
Your calculation —      строки и ее сохранения в переменной your_calculation. В качестве
ваш расчет              выражения мы ввели 12*52 (предположим, это ваш возраст, умноженный
                        на количество недель в году). Затем вызвали функцию eval, которая вы-
                        числила результат: 624.

                        Функция exec
Exec —                  Функция exec похожа на eval, но с ее помощью можно выполнять более
от execute —            сложный код. Кроме того, exec, в отличие от eval, не возвращает значе-
выполнять
                        ния. Например:


My small                          >>> my_small_program = '''print('бутерброд')
program — моя                     print('с колбасой')'''
маленькая                         >>> exec(my_small_program)
программа                         бутерброд
                                  с колбасой




116     Часть I. Учимся программировать