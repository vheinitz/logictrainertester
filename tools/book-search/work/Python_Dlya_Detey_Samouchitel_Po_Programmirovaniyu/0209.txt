self.y = -3
          if self.hit_paddle(pos) == True:
              self.y = -3
          if pos[0] <= 0:
              self.x = 3
              if pos[2] >= self.canvas_width:
              self.x = -3



   Если hit_paddle возвращает True, мы меняем направление полета
мяча, задавая свойству y значение –3 (self.y = –3). Не пытайтесь сейчас
запустить игру — мы еще не создали функцию hit_paddle. Давайте это
исправим.
   Добавьте код функции hit_paddle сразу перед функцией draw:

    def hit_paddle(self, pos):
        paddle_pos = self.canvas.coords(self.paddle.id)
        if pos[2] >= paddle_pos[0] and pos[0] <= paddle_pos[2]:
            if pos[3] >= paddle_pos[1] and pos[3] <= paddle_pos[3]:
             return True
         return False


    В строке  объявляем функцию с аргументом pos, в котором будем
передавать текущие координаты мяча. В строке  получаем координаты
ракетки и сохраняем их в переменной paddle_pos.
    В строке  находится конструкция if, условие которой означает:
x-координата правой стороны мяча больше, чем x-координата левой сто-
роны ракетки, и x-координата левой стороны мяча меньше, чем x-коорди-
ната правой стороны ракетки? Здесь pos[2] соответствует x-координате
правой стороны мяча, а pos[0] — x-координате его левой стороны. При
этом paddle_pos[0] соответствует x-координате левой стороны ракет-
ки, а paddle_pos[2] — х-координате ее правой стороны. Вот схема, где
показаны эти координаты для случая, когда мяч вот-вот коснется ракетки.




           pos[0]     pos[2]

                        paddle_pos[0]           paddle_pos[2]




                                                         14. Доделываем первую игру: «Прыг-скок!»   209