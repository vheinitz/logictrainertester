Вот как это работает: вызвав функцию time в первый раз, мы сохра-
нили значение, которое она вернула, в переменной t1 (строка с меткой ).
Затем в цикле, который начинается со строки , напечатали числа от 0
до 999. После цикла, в строке , снова вызвали time, сохранив результат
в переменной t2. Поскольку работа цикла занимает определенное вре-
мя, значение переменной t2 (количество секунд, прошедших с 1 января
1970 года) будет больше, чем значение t1. Вычтя t1 из t2 в строке , мы
узнали, сколько секунд потребовалось на печать всех чисел.

Преобразование дат с помощью asctime
Функция asctime принимает дату, заданную в виде кортежа, и преоб-              Asc time — узнать
разует ее в более понятный вид (напомню, что кортеж похож на список,           время
но его элементы нельзя менять). Как мы уже видели в главе 7, вызов
asctime без аргументов возвращает текущие дату и время в читаемом
виде.


     >>> import time
     >>> print(time.asctime())
     Mon Mar 11 22:03:41 2013                                                  Mon Tue Wed Thu
                                                                               Fri Sat Sun — дни
                                                                               недели (первые
   Чтобы вызвать asctime с аргументом, создадим кортеж с параме-               три буквы),
                                                                               начиная с поне-
трами даты и времени, сохранив его в переменной t:
                                                                               дельника
                                                                               Jan Feb Mar Apr
     >>> t = (2020, 2, 23, 10, 30, 48, 6, 0, 0)                                May Jun Jul Aug
                                                                               Sep Oct Nov
                                                                               Dec — месяцы
   Значения в кортеже соответствуют году, месяцу, дню, часам, мину-            (первые три
там, секундам, дню в неделе (0 — понедельник, 1 — вторник и так далее),        буквы), начиная
                                                                               с января
дню в году (мы указали 0 в качестве «заглушки») и режиму летнего вре-
мени (0 — нет, 1 — да). Передав этот кортеж функции asctime, получим:


     >>> import time
     >>> t = (2020, 2, 23, 10, 30, 48, 6, 0, 0)
     >>> print(time.asctime(t))
     Sun Feb 23 10:30:48 2020



Получаем дату и время с помощью localtime
Функция localtime возвращает текущие дату и время в виде объекта,              Local time — мест-
содержащего значения примерно того же вида, что и кортеж, который              ное время
принимает функция asctime. Напечатав объект, мы увидим имя клас-
са, к которому он принадлежит, а также перечень значений, помеченных



                                                                  10. Полезные модули Python   139