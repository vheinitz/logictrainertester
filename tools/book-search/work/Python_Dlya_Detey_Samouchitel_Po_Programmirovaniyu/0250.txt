game.canvas.bind_all('<KeyPress-Left>', self.turn_left)
                                               game.canvas.bind_all('<KeyPress-Right>', self.turn_right)
                                               game.canvas.bind_all('<space>', self.jump)

                                        def turn_left(self, evt):
                                            if self.y == 0:
                                                self.x = -2

                                        def turn_right(self, evt):
                                            if self.y == 0:
                                                self.x = 2



                          При нажатии клавиши-стрелки «влево» Python будет вызывать функ-
                       цию turn_left, передавая в нее объект, содержащий информацию о со-
                       бытии (аргумент evt).

                                 Для наших целей объект события не нужен, однако соответствую-
                         !       щий ему аргумент должен быть у обеих функций (строки  и ).
                                 Иначе Python, попытавшись передать этот аргумент, выдаст
                                 ошибку. Объект события содержит x и y-координаты курсора мыши
                                 (для событий мыши), код нажатой клавиши (для событий клавиа-
                                 туры) и прочую информацию. В нашей игре эти сведения не приго-
                                 дятся, поэтому на аргумент evt можно не обращать внимания.

                           Убедиться в том, что фигурка не находится в прыжке, поможет про-
                       верка свойства y в строках  и . Прыжку или падению соответствует
                       его ненулевое значение. Если y равен 0, задаем свойству x значение –2
                       (для поворота влево, строка ) или 2 (для поворота вправо, строка ).
                       Можно было бы использовать числа 1 и –1, но тогда фигурка будет дви-
                       гаться слишком медленно. (Чтобы оценить разницу, попробуйте поме-
                       нять эти значения, когда мы закончим анимацию человечка.)

                       Прыжок фигурки
Jump — прыгнуть        Функция для прыжка называется jump, она очень похожа на функции
                       turn_left и turn_right.


                                        def turn_right(self, evt):
                                            if self.y == 0:
                                                self.x = 2

                                        def jump(self, evt):
                                           if self.y == 0:
                                               self.y = -4
                                               self.jump_count = 0




250    Часть III. Пишем игру «Человечек спешит к выходу»