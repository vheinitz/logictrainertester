Непредсказуемость игры                                                             369


имеющихся у него в руках. Ведь игрок выбирает из выводимого на экран списка, в ко-
тором в принципе не может появиться четверка зефиров, так как в колоде такая карта
попросту отсутствует.
Поэтому рассмотрим код, принимающий и проверяющий выбор игрока (листинг 23.8).

 Листинг 23.8. Сделанный игроком ход
 print "Ваши действия? ",
 response = raw_input ("Введите имя карты или 'Draw', чтобы взять карту: " )
 valid_play = False                    Выполняется, пока игрок
 while not valid_play:                 не укажет корректное значение
       selected_card = None
       while selected_card == None:            Получаем от игрока сведения о карте
            if response.lower() == 'draw': или выдаем дополнительную карту
                 valid_play = True                             ÊВ
                                                                 случае значения
                 if len(deck) > 0:                              draw берем карту из
                     card = random.choice(deck)                 колоды и добавляем ее
 Получено                                                       к картам игрока
 значение draw,      p_hand.append(card)
 поэтому             deck.remove(card)
 возвращаемся        print "Вы взяли", card.short_name
 в основной цикл                                                   Проверяем наличие
                 else:
                                                                   выбранной карты
                     print "В колоде больше нет карт"              у игрока до тех пор,
                     blocked += 1                                  пока не будет на-
                 return                                            звана одна из имею-
                                                                   щихся на руках карт
            else:                                                  или не взята допол-
                 for card in p_hand:                               нительная карта
                     if response.upper() == card.short_name:
                         selected_card = card
                 if selected_card == None:
                     response = raw_input("Этой карты у вас нет. Повторите:")
                                                   Игра восьмеркой
      if selected_card.rank == '8':         корректна всегда
          valid_play = True
                                                  Проверяем, совпадает ли масть
          is_eight = True                         выложенной карты с мастью
      elif selected_card.suit == active_suit:     открытой карты
          valid_play = True
      elif selected_card.rank == up_card.rank:     Проверяем, совпадает ли
          valid_play = True                        достоинство выложенной карты
                                                         с достоинством открытой
                                                         карты
      if not valid_play:
          response = raw_input("Этот ход недопустим. Повторите попытку: ")

(Еще раз напоминаю, что это не полная версия программы, запускать ее нельзя.)