Графика                                                                       211


     width = random.randint(0, 250)
     height = random.randint(0, 100)
     top = random.randint(0, 400)
     left = random.randint(0, 500)
 pygame.draw.rect(screen, [0,0,0], [left, top, width, height], 1)
 pygame.display.flip()
 running = True
 while running:
     for event in pygame.event.get():
         if event.type == pygame.QUIT:
             running = False
 pygame.quit()

Запустите код и посмотрите, что получится. Результат должен выглядеть примерно так:




Понимаете, как работает эта программа? Она рисует 100 прямоугольников произвольного
размера и в произвольных местах. Чтобы сделать картину еще более «художественной»,
добавьте цвет и сделайте произвольной толщину линий, как в листинге 16.6.

 Листинг 16.6. Современное искусство в цвете
 import pygame, sys, random
 from pygame.color import THECOLORS
 pygame.init()
 screen = pygame.display.set_mode([640,480])
 screen.fill([255, 255, 255])