236                                                                       Глава 17



НОВЫЕ СЛОВА                            Библиотека Pygame позволяет также группи-
                                       ровать спрайты. Например, в модели игры
  Распознавание столкновений озна-     в боу­линг кегли можно поместить в одну груп-
  чает получение информации о со-      пу, а шар — в другую.
  прикосновении или перекрывании
  двух спрайтов. Когда два дви-        Группирование идет рука об руку с распознава-
  жущихся объекта натыкаются друг      нием столкновений. В примере с игрой в боу-
  на друга, это называется стол-       линг нам важен момент соприкосновения шара
  кновением.
                                       с любой кеглей, поэтому проверяться будет
                                       столкновение спрайта, представляющего шар,
с любым спрайтом из группы кеглей. Также можно распознавать столкновения внутри
группы (столкновения кеглей друг с другом).
Рассмотрим пример. Начнем с отскакивающих от стен мячей, но для простоты будем
рассматривать всего четыре мяча вместо девяти. А вместо списка мячей, которым мы
пользовались в последнем примере, прибегнем к Pygame-классу group.
Кроме того, мы немного почистим код, поместив связанную с анимацией мяча часть кода
(последние несколько строк из листинга 17.2) в функцию с именем animate(). Именно
там будет располагаться код распознавания столкновений. После столкновения мячи
будут менять направление движения (листинг 17.3).

 Листинг 17.3. Распознавание столкновений с использование
 группы спрайтов вместо списка
 import sys, pygame
 from random import *

 class MyBallClass(pygame.sprite.Sprite):
     def __init__(self, image_file, location, speed):
         pygame.sprite.Sprite.__init__(self)
         self.image = pygame.image.load(image_file)
         self.rect = self.image.get_rect()
         self.rect.left, self.rect.top = location
         self.speed = speed                                          Определение
                                                                     класса мячей
      def move(self):
          self.rect = self.rect.move(self.speed)
          if self.rect.left < 0 or self.rect.right > width:
              self.speed[0] = -self.speed[0]
          if self.rect.top < 0 or self.rect.bottom > height:
              self.speed[1] = -self.speed[1]