{
  "name": "logik-trainer",
  "version": "0.2.0",
  "description": "LOGIK-Trainer – kognitives Trainings- und Testprogramm für Kinder (KABC-II-angelehnt)",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node tools/build.mjs",
    "build:dev": "node tools/build.mjs --dev",
    "watch": "node tools/build.mjs --watch",
    "serve": "python3 -m http.server 8080",
    "test": "node test/smoke.mjs && npm run build && node test/integration.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.28.0",
    "fake-indexeddb": "^6.2.5",
    "jsdom": "^30.0.1"
  }
}
