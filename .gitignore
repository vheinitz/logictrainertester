node_modules/
.backup/
*.pdf
.DS_Store

# dist/ wird bewusst eingecheckt: die App soll ohne Node/Build direkt per
# Doppelklick auf index.html laufen (Offline-Nutzung auf Tablet/Familien-PC).
# Neu erzeugen mit: npm run build
!dist/

# Arbeitsklone und Zwischenstände, kein Quellcode
tmp/

# Auslieferungsordner, wird erzeugt: node tools/make-deploy.mjs
deploy/
